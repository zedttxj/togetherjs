services:
  - type: web
    name: togetherjs-hub
    env: node
    plan: free
    rootDir: .
    buildCommand: npm install
    startCommand: node hub/server.js
    envVars:
      - key: NODE_VERSION
        value: 18
